<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jonathanev.review">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- BUGS
         1.- Hay que verificar que el botón de eliminar funcione correctamente.
         (SOLUCIONADO)

         2.- Si agregamos los colores  sin orden el archivo no se crea o modifica correctamente,
             ya sea en el apartado de crear una nueva guia o en modificar.

         NOTA: El botón atras funciona pero unicamente antes de agregar mas preguntas,
         cuando llegas a esa opción no puedes usar el botón de atrás sino no jala.
         (SOLUCIONADO)

         3.- Si cambias de color 2 veces en la misma palabra se guardan colores antiguos y se hace
             una revoltura (Este aún no lo verifico exactamente).

         MEJORAS
         4.- Hay que quitar un botón de btnGuardarGuia, que el código quede en uno solo.
         (SOLUCIONADO)

         5.- Al momento de estar modificando una guía no tenemos botón de atras y estaría bueno
             implementarlo.
         (SOLUCIONADO)

         6.- Cambiar los botónes de siguiente y atras por iconos con flecha de siguiente y atras.

         7.- Que puedas guardar las guías directamente desde la primer pregunta.
         (SOLUCIONADO)

         8.- Optimización del código, exactamente la optimización es en las preguntas modificadas,
             hay unas bifurcaciones que jamás entran y algunas cosas que no se utilizan.
         (SOLUCIONADO)

         9.- Cuando recien instalas la aplicación no tienes la carpeta de files, hay que hacer que
             en cuanto la abras te cree la carpeta.
         (SOLUCIONADO)

         10.- No se puede cambiar colores a letras que ya tenía un color anteriormente.
    -->
    <application
        android:allowBackup="true"
        android:icon="@drawable/estudiantes"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Review">
        <activity
            android:name="com.jonathanev.review.Activities.Activity_Modificar"
            android:exported="false" />
        <!-- Sample AdMob app ID: ca-app-pub-3940256099942544~3347511713 -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-5116088101296740~3054801927" />

        <activity
            android:name="com.jonathanev.review.Activities.Activity_RepasarGuia"
            android:exported="false"
            android:screenOrientation="userPortrait" />
        <activity
            android:name="com.jonathanev.review.Activities.Activity_Cuestionario"
            android:exported="true"
            android:screenOrientation="userPortrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.jonathanev.review.MainActivity"
            android:exported="true"
            android:screenOrientation="userPortrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>